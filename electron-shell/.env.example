# Gordon — environment config
#
# Abhay: copy this file to `.env` in this folder and paste your key below.
# The app reads it on launch; no code changes needed. `.env` is gitignored,
# so the real key never gets committed.
#
#   cp .env.example .env
#   # then edit .env and set the key
#
ANTHROPIC_API_KEY=
